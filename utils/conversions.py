import math

def degrees_to_radians(deg_angle):
    rad_angle = math.radians(deg_angle)
    return rad_angle

def radians_to_degrees(rad_angle):
    deg_angle = math.degrees(rad_angle)
    return deg_angle
